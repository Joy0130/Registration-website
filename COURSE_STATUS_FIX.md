# 課程狀態即時更新修復

## 問題描述
原本系統中，當課程開放報名時間到達（例如14:30），用戶重新整理頁面時狀態沒有立即從「尚未開放」變成「報名中」，需要等待定時任務執行才會更新。

## 問題原因
1. **定時任務頻率不夠高**：原本每1分鐘檢查一次，可能導致最多1分鐘的延遲
2. **缺乏即時檢查機制**：只依賴定時任務，頁面載入時沒有主動檢查狀態

## 解決方案

### 1. 即時狀態檢查
在關鍵的API路由中添加即時狀態檢查：

- `/api/courses` - 取得課程列表時
- `/api/my_courses` - 取得我的課程時  
- `/course/<id>` - 查看課程詳情時

這些路由會在返回資料前先執行 `check_course_status()`，確保狀態是最新的。

### 2. 提高定時任務頻率
將定時任務間隔從1分鐘縮短為30秒，減少最大延遲時間。

### 3. 改進日誌輸出
增強 `check_course_status()` 函數的日誌輸出：
- 顯示檢查時間戳
- 顯示具體的開始/結束時間
- 統計更新的課程數量
- 區分不同類型的狀態更新

### 4. 手動觸發API
新增 `/api/check_course_status` API端點，允許前端手動觸發狀態檢查。

## 修改的檔案

### app.py
```python
# 1. 在關鍵API中添加即時檢查
@app.route('/api/courses', methods=['GET'])
def get_courses():
    check_course_status()  # 新增
    # ... 原有邏輯

# 2. 縮短定時任務間隔
scheduler.add_job(check_course_status, 'interval', seconds=30)  # 從 minutes=1 改為 seconds=30

# 3. 新增手動觸發API
@app.route('/api/check_course_status', methods=['POST'])
def manual_check_course_status():
    # ... 實作邏輯
```

## 測試工具

### test_course_status.py
提供完整的測試功能：
- 自動創建測試課程並驗證狀態更新
- 手動執行狀態檢查
- 顯示當前所有課程的狀態

### 使用方法
```bash
python3 test_course_status.py
```

## 效果
修復後，用戶在課程開放時間重新整理頁面時：
1. **立即看到狀態更新**：API會先檢查狀態再返回資料
2. **更短的延遲**：即使依賴定時任務，最大延遲也從60秒縮短為30秒
3. **更好的可觀測性**：詳細的日誌輸出便於除錯

## 注意事項
1. 即時檢查會增加一些資料庫查詢，但對於一般使用量來說影響很小
2. 30秒的定時任務頻率在正常情況下不會造成效能問題
3. 如果需要更即時的更新，可以考慮在前端添加輪詢或使用WebSocket